openapi: "3.0.0"
info:
  title: TODO API Document
  version: "0.0.1"
  description: TODO API のドキュメントです
tag:
  - name: opts
    description: 運用監視関連API
  - name: tasks
    description: タスク関連のAPI
paths:
  /health:
    get:
      tag:
        - opts
      responses:
        '200':
          description: OK
  /tasks/1:
    get:
      tags:
        - tasks
      responses:
        '200':
          description: OK